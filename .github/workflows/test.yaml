name: Test
on: [push, pull_request]
jobs:
  test:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v4
    - name: make test
      run: |
        cd test
        make test
    - name: run test
      run: ./test
